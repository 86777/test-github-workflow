name: Workflow_01
on:
  - push
jobs:
  Wersja_Pythona:
    runs-on: ubuntu-latest
    steps:
      - run: python --version
  Testowanie_w_Pylint:
    runs-on: ubuntu-latest
    steps:
      - run: pylint --version
